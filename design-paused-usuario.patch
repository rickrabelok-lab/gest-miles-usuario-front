diff --git a/index.html b/index.html
index b5c1486..7bdd8ee 100644
--- a/index.html
+++ b/index.html
@@ -11,7 +11,7 @@
       content="Gest Miles — gestão de milhas e pontos de fidelidade."
     />
     <meta name="author" content="Gest Miles" />
-    <meta name="theme-color" content="#0d9488" />
+    <meta name="theme-color" content="#8A05BE" />
 
     <!-- Open Graph (preview em WhatsApp, Telegram, etc.) -->
     <meta property="og:title" content="Gest Miles" />
@@ -40,7 +40,7 @@
     />
   </head>
 
-  <body>
+  <body style="margin:0;background:#f8f7f8;">
     <div id="root"></div>
     <script type="module" src="/src/main.tsx"></script>
   </body>
diff --git a/src/App.tsx b/src/App.tsx
index f97c757..aeed7cf 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -33,6 +33,7 @@ import RadarOportunidadesPage from "./pages/RadarOportunidadesPage";
 import ClienteInsightsPage from "./pages/ClienteInsightsPage";
 import ClienteTimelinePage from "./pages/ClienteTimelinePage";
 import AssinaturaClientePage from "./pages/AssinaturaClientePage";
+import VisualLoadingPreview from "./pages/VisualLoadingPreview";
 import { AuthProvider, useAuth } from "@/contexts/AuthContext";
 import { SearchFlightsProvider } from "@/contexts/SearchFlightsContext";
 import RequireAuth from "@/components/RequireAuth";
@@ -90,6 +91,7 @@ const App = () => {
           <BrowserRouter>
             <Routes>
               <Route path="/" element={<HomeGate />} />
+              <Route path="/visual-loading-preview" element={<VisualLoadingPreview />} />
               <Route
                 path="/assinatura"
                 element={
diff --git a/src/components/BalanceTabs.tsx b/src/components/BalanceTabs.tsx
index 500898d..8ccb4df 100644
--- a/src/components/BalanceTabs.tsx
+++ b/src/components/BalanceTabs.tsx
@@ -46,7 +46,7 @@ const BalanceTabs = ({
   });
 
   return (
-    <div className="flex overflow-x-auto border-b border-gray-200 px-5 scrollbar-hide">
+    <div className="flex gap-2 overflow-x-auto border-b border-zinc-200/80 bg-[#f8f7f8] px-5 pb-0 scrollbar-hide">
       {visibleTabs.map((tab) => {
         const isActive = activeTab === tab.id;
         const Icon = tab.icon;
@@ -55,9 +55,9 @@ const BalanceTabs = ({
           <button
             key={tab.id}
             onClick={() => onTabChange(tab.id)}
-            className={`-mb-px flex shrink-0 items-center justify-center gap-1 border-b-2 px-3 py-2.5 text-[11px] font-medium transition-colors ${
+            className={`-mb-px flex shrink-0 items-center justify-center gap-1 rounded-t-2xl border-b-2 px-3 py-2.5 text-[12px] font-medium transition-all ${
               isActive
-                ? "border-[#8A05BE] font-bold text-[#8A05BE]"
+                ? "border-[#8A05BE] bg-white/70 font-bold text-[#8A05BE] shadow-[0_8px_18px_rgba(138,5,190,0.08)]"
                 : "border-transparent text-gray-400 hover:text-gray-600"
             }`}
           >
diff --git a/src/components/BottomNav.tsx b/src/components/BottomNav.tsx
index 9190296..028e012 100644
--- a/src/components/BottomNav.tsx
+++ b/src/components/BottomNav.tsx
@@ -127,13 +127,13 @@ const BottomNav: FC<BottomNavProps> = () => {
   }, [searchParams]);
 
   return (
-    <div className="pointer-events-auto fixed bottom-0 left-0 right-0 z-[100] border-t border-stone-200/80 bg-white shadow-[0_-2px_16px_rgba(0,0,0,0.06)]">
+    <div className="pointer-events-auto fixed bottom-0 left-0 right-0 z-[100] border-t border-stone-200/70 bg-white/90 shadow-[0_-8px_26px_rgba(0,0,0,0.08)] backdrop-blur-xl">
       <div className="mx-auto flex max-w-md items-stretch justify-between gap-0 px-1.5 py-1.5 sm:px-2">
         {NAV.map(({ id, label, Icon }) => {
           const isActive = active === id;
           const tabClass = cn(
             "flex w-full flex-col items-center justify-center gap-0.5 rounded-lg py-1.5 transition-colors no-underline",
-            isActive ? "text-nubank-primary" : "text-stone-500 hover:text-stone-800",
+            isActive ? "text-[#8A05BE]" : "text-stone-500 hover:text-stone-800",
           );
 
           if (id === "passagens") {
@@ -149,7 +149,7 @@ const BottomNav: FC<BottomNavProps> = () => {
                 >
                   <span className="relative">
                     <Icon
-                      className="h-5 w-5 shrink-0"
+                      className={cn("h-5 w-5 shrink-0", isActive && "drop-shadow-[0_0_8px_rgba(138,5,190,0.34)]")}
                       strokeWidth={isActive ? 2.25 : 1.75}
                       aria-hidden
                     />
@@ -190,7 +190,7 @@ const BottomNav: FC<BottomNavProps> = () => {
               >
                 <span className="relative">
                   <Icon
-                    className="h-5 w-5 shrink-0"
+                    className={cn("h-5 w-5 shrink-0", isActive && "drop-shadow-[0_0_8px_rgba(138,5,190,0.34)]")}
                     strokeWidth={isActive ? 2.25 : 1.75}
                     aria-hidden
                   />
diff --git a/src/components/DashboardHeader.tsx b/src/components/DashboardHeader.tsx
index ced3701..546183f 100644
--- a/src/components/DashboardHeader.tsx
+++ b/src/components/DashboardHeader.tsx
@@ -274,14 +274,14 @@ const DashboardHeader = () => {
   };
 
   return (
-    <div className="border-b border-border/20 bg-transparent text-foreground">
-      <div className="flex items-center justify-between px-4 py-2.5">
+    <div className="bg-[#f8f7f8] text-foreground">
+      <div className="flex items-center justify-between px-4 pb-2 pt-3">
         <div className="flex items-center gap-2">
           <DropdownMenu>
             <DropdownMenuTrigger asChild>
               <button
                 type="button"
-                className="relative flex h-[38px] w-[38px] shrink-0 items-center justify-center rounded-full border-2 border-background bg-gradient-to-br from-[#8A05BE] to-[#a855f7] text-[13px] font-extrabold text-white shadow-[0_2px_12px_rgba(138,5,190,0.40)] transition-all duration-200 hover:scale-105 hover:shadow-[0_4px_20px_rgba(138,5,190,0.55)] active:scale-95 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/40"
+                className="relative flex h-[42px] w-[42px] shrink-0 items-center justify-center rounded-full border-2 border-white bg-gradient-to-br from-[#8A05BE] via-[#a100d7] to-[#d946ef] text-[13px] font-extrabold text-white shadow-[0_8px_24px_rgba(138,5,190,0.42)] transition-all duration-200 hover:scale-105 hover:shadow-[0_10px_28px_rgba(138,5,190,0.55)] active:scale-95 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/40"
                 aria-label="Menu do usuário"
               >
                 {user ? getInitials(user.email) : "?"}
@@ -351,12 +351,12 @@ const DashboardHeader = () => {
               <img
                 src={optionalHeaderWordmarkImageUrl}
                 alt="GestMiles"
-                className="h-8 w-auto max-w-[min(160px,42vw)] object-contain object-center sm:h-9 sm:max-w-[180px]"
+                className="h-9 w-auto max-w-[min(170px,44vw)] object-contain object-center sm:h-10 sm:max-w-[190px]"
                 loading="eager"
                 decoding="async"
               />
             ) : (
-              <span className="font-display text-[22px] font-bold leading-none tracking-tight sm:text-2xl">
+              <span className="font-display text-[28px] font-black leading-none tracking-tight sm:text-3xl">
                 <span className="text-foreground">Gest</span>
                 <span className="text-[#8A05BE]">Miles</span>
               </span>
@@ -367,7 +367,7 @@ const DashboardHeader = () => {
           <SheetTrigger asChild>
             <button
               type="button"
-              className="rounded-xl p-2 text-muted-foreground transition-all duration-200 hover:bg-muted hover:text-foreground"
+              className="rounded-xl p-2 text-zinc-600 transition-all duration-200 hover:bg-white hover:text-foreground"
               aria-label="Abrir menu"
             >
               <Menu size={22} />
@@ -607,13 +607,13 @@ const DashboardHeader = () => {
       </div>
 
       {bannerVisible && (
-        <div className="mx-4 mb-2.5 flex items-center gap-2 rounded-xl border border-amber-200 bg-amber-50 px-3 py-2 text-[11px] text-amber-800">
+        <div className="mx-4 mb-3 flex items-center gap-2 rounded-2xl border border-amber-200/70 bg-[#fffdf3] px-3.5 py-2.5 text-[12px] text-amber-900 shadow-[0_6px_18px_rgba(120,53,15,0.06)]">
           <Zap size={14} className="shrink-0 text-amber-600" aria-hidden />
           {showDemandOpenBanner ? (
             <button
               type="button"
               onClick={goDemandasPendentes}
-              className="flex-1 text-left text-[11px] text-amber-800 hover:opacity-90"
+              className="flex-1 text-left text-[12px] text-amber-900 hover:opacity-90"
             >
               <b>Demandas abertas:</b> {demandSummary.openCount}{" "}
               {demandSummary.openCount === 1 ? "demanda" : "demandas"}{" "}
@@ -622,7 +622,7 @@ const DashboardHeader = () => {
                 : "no momento."}
             </button>
           ) : (
-            <p className="flex-1 text-[11px] leading-snug text-amber-800">
+            <p className="flex-1 text-[12px] leading-snug text-amber-900">
               Bônus de até <span className="font-bold text-amber-700">133%</span> na transferência. Confira
             </p>
           )}
diff --git a/src/components/HomeGate.tsx b/src/components/HomeGate.tsx
index 759f4d1..e238bb5 100644
--- a/src/components/HomeGate.tsx
+++ b/src/components/HomeGate.tsx
@@ -4,6 +4,7 @@ import { useAuth } from "@/contexts/AuthContext";
 import RequireClienteApp from "@/components/RequireClienteApp";
 import Index from "@/pages/Index";
 import { adminAppBaseUrl } from "@/lib/staffAppUrls";
+import { GestMilesSplashLoading } from "@/components/brand/GestMilesLoadings";
 
 /**
  * `/` — clientes veem o dashboard; admin é enviado para o painel Admin (outro front).
@@ -12,11 +13,7 @@ const HomeGate = () => {
   const { user, loading, role, roleLoading } = useAuth();
 
   if (loading || roleLoading) {
-    return (
-      <div className="flex min-h-screen items-center justify-center text-sm text-muted-foreground">
-        A carregar...
-      </div>
-    );
+    return <GestMilesSplashLoading />;
   }
 
   if (!user) {
diff --git a/src/components/ProgramCard.tsx b/src/components/ProgramCard.tsx
index 2f15c95..f022f5e 100644
--- a/src/components/ProgramCard.tsx
+++ b/src/components/ProgramCard.tsx
@@ -88,7 +88,7 @@ const ProgramCard = (props: ProgramCardProps) => {
 
   return (
     <div
-      className="relative cursor-pointer rounded-xl border border-[#EBEBEB] bg-white p-3 text-nubank-text outline-none transition-colors focus-visible:ring-2 focus-visible:ring-primary/20 active:scale-[0.99]"
+      className="relative min-h-[126px] cursor-pointer rounded-[18px] border border-[#ECE7EF] bg-white p-3.5 text-nubank-text shadow-[0_10px_26px_rgba(17,24,39,0.06)] outline-none transition-all hover:-translate-y-0.5 hover:shadow-[0_14px_32px_rgba(17,24,39,0.09)] focus-visible:ring-2 focus-visible:ring-primary/20 active:scale-[0.99]"
       role="button"
       tabIndex={0}
       onClick={handleOpenDetails}
@@ -102,8 +102,8 @@ const ProgramCard = (props: ProgramCardProps) => {
         <div
           className={
             hasBrandImage
-              ? "flex h-9 w-9 shrink-0 items-center justify-center overflow-hidden rounded-xl bg-white p-0.5 ring-1 ring-black/[0.06]"
-              : "flex h-9 w-9 shrink-0 items-center justify-center overflow-hidden rounded-xl text-[11px] font-bold ring-1 ring-black/[0.04]"
+              ? "flex h-11 w-11 shrink-0 items-center justify-center overflow-hidden rounded-2xl bg-white p-1 ring-1 ring-black/[0.06] shadow-[0_6px_16px_rgba(17,24,39,0.06)]"
+              : "flex h-11 w-11 shrink-0 items-center justify-center overflow-hidden rounded-2xl text-[11px] font-bold ring-1 ring-black/[0.04] shadow-[0_6px_16px_rgba(17,24,39,0.05)]"
           }
           style={
             hasBrandImage
@@ -128,21 +128,21 @@ const ProgramCard = (props: ProgramCardProps) => {
         </div>
 
         {variation === "up" && (
-          <span className="rounded bg-green-50 px-1.5 py-0.5 text-[8px] font-bold text-green-700">↑</span>
+          <span className="rounded-full bg-emerald-50 px-2 py-1 text-[10px] font-bold text-emerald-600">↑</span>
         )}
         {variation === "down" && (
-          <span className="rounded bg-red-50 px-1.5 py-0.5 text-[8px] font-bold text-red-600">↓</span>
+          <span className="rounded-full bg-red-50 px-2 py-1 text-[10px] font-bold text-red-600">↓</span>
         )}
         {variation === "none" && (
-          <span className="rounded bg-gray-50 px-1.5 py-0.5 text-[8px] font-bold text-gray-300">—</span>
+          <span className="rounded-full bg-gray-50 px-2 py-1 text-[10px] font-bold text-gray-300">—</span>
         )}
       </div>
 
-      <div className="mb-1 font-extrabold tabular-nums leading-tight tracking-tight text-gray-900" style={{ fontSize: "15px" }}>
+      <div className="mb-1 mt-2 font-extrabold tabular-nums leading-tight tracking-tight text-gray-950" style={{ fontSize: "20px" }}>
         {balance}
       </div>
 
-      <div className="leading-tight text-gray-400" style={{ fontSize: "9px" }}>
+      <div className="leading-tight text-gray-400" style={{ fontSize: "11px" }}>
         R$ {valueInBRL} · {lastUpdate}
       </div>
 
diff --git a/src/pages/Index.tsx b/src/pages/Index.tsx
index d2ace17..6f0db9c 100644
--- a/src/pages/Index.tsx
+++ b/src/pages/Index.tsx
@@ -2008,11 +2008,11 @@ const Index = () => {
   };
 
   return (
-    <div className="mx-auto min-h-screen max-w-md bg-nubank-bg pb-28">
+    <div className="mx-auto min-h-screen max-w-md bg-[#f8f7f8] pb-28 shadow-[0_0_36px_rgba(17,24,39,0.08)]">
       <DashboardHeader />
 
       {managerMode && !managerClientId && !demandBannerDismissed && (
-        <div className="mx-4 mb-1 flex items-center gap-2 rounded-xl border border-amber-200 bg-amber-50 px-3 py-2 text-[11px] text-amber-800">
+        <div className="mx-4 mb-3 flex items-center gap-2 rounded-2xl border border-amber-200/70 bg-[#fffdf3] px-3.5 py-2.5 text-[12px] text-amber-900 shadow-[0_6px_18px_rgba(120,53,15,0.06)]">
           <span className="shrink-0">⚡</span>
           <button
             type="button"
@@ -2175,7 +2175,7 @@ const Index = () => {
                     <button
                       type="button"
                       onClick={() => setIsAddProgramMenuOpen(true)}
-                      className="inline-flex h-9 items-center justify-center gap-1 rounded-[10px] border border-[#8A05BE] bg-white px-3 text-[11px] font-semibold whitespace-nowrap text-[#8A05BE] shadow-nubank transition-colors hover:bg-purple-50 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-200 dark:hover:bg-slate-700"
+                      className="inline-flex h-11 items-center justify-center gap-1 rounded-2xl border border-[#8A05BE] bg-white px-4 text-[12px] font-semibold whitespace-nowrap text-[#8A05BE] shadow-[0_8px_20px_rgba(138,5,190,0.08)] transition-colors hover:bg-purple-50 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-200 dark:hover:bg-slate-700"
                     >
                       <Plus size={12} />
                       <span>Novo</span>
@@ -2201,7 +2201,7 @@ const Index = () => {
                         <button
                           type="button"
                           onClick={() => setIsDemandDialogOpen(true)}
-                          className="inline-flex h-9 flex-1 items-center justify-center rounded-[10px] border border-transparent bg-primary px-2 text-[11px] font-semibold whitespace-nowrap text-primary-foreground shadow-nubank transition-colors hover:bg-primary/90"
+                          className="inline-flex h-11 flex-1 items-center justify-center rounded-2xl border border-transparent bg-gradient-to-r from-[#8A05BE] to-[#b000d4] px-2 text-[12px] font-bold whitespace-nowrap text-white shadow-[0_12px_24px_rgba(138,5,190,0.28)] transition-all hover:-translate-y-0.5 hover:shadow-[0_14px_28px_rgba(138,5,190,0.34)]"
                         >
                           Solicitar Cotação
                         </button>
