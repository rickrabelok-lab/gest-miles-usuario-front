# Build do app mobile (Capacitor): `npm run build:mobile` (vite build --mode mobile).
# No Vite, .env.[mode] tem prioridade sobre .env.local — só o override entra aqui.
# Dentro do APK não existe proxy /api do Vite dev, então a URL do BFF precisa ser absoluta.
# Só valores PÚBLICOS neste arquivo (é versionado); segredos continuam em .env.local/backend.
VITE_API_URL=https://gest-miles-usuario-front-slzj.vercel.app
