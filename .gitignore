# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# TypeScript incremental build cache (regenerado por `tsc -b`)
*.tsbuildinfo


.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# MCP local (pode conter chaves; não versionar)
.cursor/mcp.json

# Git worktrees
.worktrees/

.vercel

# Supabase local CLI state
supabase/.temp/

# Scripts ad-hoc com PII real (email/nome de clientes/staff) — SEC-007.
# Untrackados em 2026-06-03 (mantidos no disco local). Não versionar dados de cliente.
supabase/RUN_EQUIPES_1_E_2_GESTORES.sql
supabase/RUN_EQUIPE_CS_TRES_RESPONSAVEIS_AMBAS_EQUIPES.sql

# Scratch local da skill de brainstorming (mockups HTML + server.pid) — não versionar.
.superpowers/
