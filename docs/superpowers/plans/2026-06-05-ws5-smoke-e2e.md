# WS5 — Smoke E2E do cliente (Implementation Plan)

> REQUIRED SUB-SKILL: superpowers:executing-plans.

**Goal:** Smoke E2E (Playwright/TS, `@playwright/test`) do caminho feliz do cliente: login → app autenticado → tela-chave → logout, + guarda de rota (sem sessão → /auth). Durável e re-rodável (`npm run test:e2e`).

**Architecture:** `@playwright/test` (TS, combina com o repo). `baseURL` via `E2E_BASE_URL` (default `http://localhost:3081`); credenciais via `E2E_EMAIL`/`E2E_PASSWORD` (env, **nunca commitadas**; testes que logam dão `test.skip` se ausentes). Sem `webServer` no config — o runner garante o app de pé (local `npm run dev` OU uma URL deployada).

**Tech Stack:** `@playwright/test` (devDep, instalado via **pnpm** — ver [[deps-use-pnpm-not-npm]]). Chromium headless.

**Branch:** `feat/usuario-e2e-smoke`.

**Selectors (reconhecidos no código):** login `#auth-email` / `#auth-password` / botão "Entrar" (exact); header autenticado botão `aria-label="Menu do usuário"`; itens de menu "Meu perfil" / "Sair".

## File Structure

- **Create** `playwright.config.ts`, `e2e/smoke.spec.ts`.
- **Modify** `package.json` (+`@playwright/test` devDep, script `test:e2e`), `pnpm-lock.yaml` (+ `package-lock.json` p/ consistência), `.gitignore` (saídas do Playwright), `.env.example` (E2E_*).

---

## Task 1: Dependência + config + spec

- [ ] **Step 1: Adicionar `@playwright/test` via pnpm** (não npm!): `pnpm add -D @playwright/test` ; depois sincronizar o npm lockfile: `npm install --package-lock-only`.

- [ ] **Step 2: `playwright.config.ts`**

```ts
import { defineConfig, devices } from "@playwright/test";

const BASE_URL = process.env.E2E_BASE_URL || "http://localhost:3081";

export default defineConfig({
  testDir: "./e2e",
  timeout: 60_000,
  expect: { timeout: 15_000 },
  retries: process.env.CI ? 1 : 0,
  reporter: process.env.CI ? "github" : "list",
  use: { baseURL: BASE_URL, headless: true, trace: "on-first-retry" },
  projects: [{ name: "chromium", use: { ...devices["Desktop Chrome"] } }],
});
```

- [ ] **Step 3: `e2e/smoke.spec.ts`**

```ts
import { test, expect, type Page } from "@playwright/test";

const EMAIL = process.env.E2E_EMAIL;
const PASSWORD = process.env.E2E_PASSWORD;
const haveCreds = Boolean(EMAIL && PASSWORD);

async function login(page: Page) {
  await page.goto("/auth");
  await page.locator("#auth-email").waitFor({ state: "visible", timeout: 30_000 });
  await page.locator("#auth-email").fill(EMAIL!);
  await page.locator("#auth-password").fill(PASSWORD!);
  await page.getByRole("button", { name: "Entrar", exact: true }).click();
  // Login bem-sucedido sai de qualquer tela /auth.
  await expect(page).not.toHaveURL(/\/auth/, { timeout: 30_000 });
}

test.describe("smoke do app do cliente", () => {
  test("rota protegida sem sessão redireciona pro login", async ({ page }) => {
    await page.goto("/perfil");
    await expect(page.locator("#auth-email")).toBeVisible({ timeout: 30_000 });
    expect(new URL(page.url()).pathname).toContain("/auth");
  });

  test("login leva ao app autenticado", async ({ page }) => {
    test.skip(!haveCreds, "Defina E2E_EMAIL e E2E_PASSWORD para rodar.");
    await login(page);
    await page.goto("/");
    await expect(page.getByRole("button", { name: "Menu do usuário" })).toBeVisible({ timeout: 30_000 });
  });

  test("abre tela-chave (perfil) e faz logout", async ({ page }) => {
    test.skip(!haveCreds, "Defina E2E_EMAIL e E2E_PASSWORD para rodar.");
    await login(page);
    await page.goto("/");
    await page.getByRole("button", { name: "Menu do usuário" }).click();
    await page.getByRole("menuitem", { name: "Meu perfil" }).click();
    await expect(page).toHaveURL(/\/perfil/, { timeout: 30_000 });
    await page.getByRole("button", { name: "Menu do usuário" }).click();
    await page.getByRole("menuitem", { name: "Sair" }).click();
    await expect(page.locator("#auth-email")).toBeVisible({ timeout: 30_000 });
  });
});
```

- [ ] **Step 4: `package.json`** — adicionar script `"test:e2e": "playwright test"`.

- [ ] **Step 5: `.gitignore`** — adicionar:

```
# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
```

- [ ] **Step 6: `.env.example`** — adicionar:

```
# E2E (Playwright). Credenciais da conta de teste — NÃO commitar valores reais.
# E2E_BASE_URL default http://localhost:3081 (ou aponte pra uma URL deployada).
# E2E_BASE_URL=
# E2E_EMAIL=
# E2E_PASSWORD=
```

- [ ] **Step 7: Commit**

```bash
git add playwright.config.ts e2e/smoke.spec.ts package.json pnpm-lock.yaml package-lock.json .gitignore .env.example
git commit -m "test(usuario): smoke E2E do cliente (Playwright) - login/guarda/logout"
```

---

## Task 2: Verificar contra o app ao vivo

- [ ] **Step 1: Instalar o browser:** `npx playwright install chromium`
- [ ] **Step 2: Rodar contra prod** (app está no ar):

```bash
E2E_BASE_URL=https://app.gestmiles.com.br E2E_EMAIL=smoke-usuario@gestmiles.com.br E2E_PASSWORD=TesteSmoke97 npx playwright test
```
Expected: 3 passed (guarda + login + logout).

- [ ] **Step 3:** Se algum selector falhar, recon com screenshot (`--debug`/trace) e ajustar; re-rodar.

---

## Task 3: Gate + PR

- [ ] **Step 1:** `npx tsc -b` (0) — o `e2e/` é TS mas fora do build do Vite; confirmar que o tsc do app segue limpo. `npm test` (unit, verde). `npm run build` (ok).
- [ ] **Step 2:** PR.

```bash
git push -u origin feat/usuario-e2e-smoke
gh pr create --base main --title "test(usuario): smoke E2E do cliente (Playwright) — WS5" \
  --body "Smoke E2E: login → app autenticado → perfil → logout + guarda de rota. baseURL/creds via env (E2E_*), nunca commitadas; testes que logam dão skip sem creds. Verificado contra app.gestmiles.com.br com a conta de teste. Parte do launch-readiness (WS5)."
```

## Self-Review

**Cobertura (WS5):** login ✅ · tela-chave (perfil) ✅ · logout ✅ · guarda de rota (401/403 gracioso → /auth) ✅. **Segurança:** creds só por env, skip sem elas. **Placeholders:** nenhum. **Risco:** selectors de menu (Radix) — mitigado por roles/nome; verificação ao vivo confirma. `@playwright/test` via pnpm (senão quebra deploy).
